#include <stdio.h>
#include <stdlib.h>

int main()
{   
    //Dadas dos variables de cualquier tipo en un programa en C, considera la siguiente expresi´on:
    // &a == &b
    // Seria correcto pero siempre daria falso porque 2 variables nunca van a tener la misma direccion de memoria 
    // C)
    return 0; 
}